#!/bin/bash

touch .gitignore

> .gitignore
git status --porcelain | grep '^??' | cut -c4- >> .gitignore

if [[ $PWD = ~ ]]; then
    echo ".local/share/Trash" >> .gitignore
    echo ".vim/fzf/" >> .gitignore
    echo ".vim/undo/" >> .gitignore
fi

sort -u .gitignore -o .gitignore
