#!/bin/bash -v

# variable
install="pacaur -S --needed --noconfirm --noedit"

# --------------------------------

# clang
$install clang # completer: ~/.clang_complete
$install clang-format # formatter: ~/.clang-format
$install gcc-multilib # linter

# go
$install go

$install gocode-git # completer
$install gofmt goimports-git # formatter linter

# java
$install jdk8-openjdk

# nodejs
$install nodejs
$install yarn

yarn global add tern # completer: ~/.tern-config
yarn global add js-beautify prettier{,-eslint-cli} # formatter: ~/.jsbeautifyrc
yarn global add eslint{,-config-airbnb,-plugin{-import,-jsx-a11y,-react}} # linter: ~/.eslintrc

# python
$install python-pip

pip install --user jedi # completer
pip install --user yapf # formatter: ~/.style.yapf
pip install --user flake8 # linter: ~/.config/flake8

# --------------------------------

# bfs
$install bfs

# ctags
$install universal-ctags-git

# curl
$install curl

# drive
$install drive

# font
$install ttf-hack
$install ttf-symbola

# ripgrep
$install ripgrep

# tmux
$install tmux

# vim
$install neovim
$install python-neovim

curl -fLo ~/.vim/autoload/plug.vim --create-dirs \
      https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
vim +PlugInstall +qa

mkdir ~/.vim/undo

# xsel
$install xsel

# zsh
$install zsh

mkdir ~/.antigen
curl -L git.io/antigen > ~/.antigen/antigen.zsh

chsh -s $(which zsh)
