#!/bin/bash -v

# variables
install='trizen -S --needed --noconfirm --noedit'

# trizen
git clone https://aur.archlinux.org/trizen.git
cd trizen && makepkg -si --needed --noconfirm && cd .. && rm -rf trizen

# --------------------------------

# clang
# - formatter: clang-format (~/.clang-format)
# - linter: gcc
$install clang

# go
# - completer: gocode
# - formatter: gofmt, goimports
# - langserver: go-langserver
# - linter: gofmt, goimports
$install go
$install gocode-git
$install goimports-git

export GOPATH=$HOME/.go
go get -u github.com/sourcegraph/go-langserver

# html
# - formatter: html-beautify (~/.jsbeautifyrc)
# javascript/typescript
# - completer: typescript-language-server
# - formatter: prettiereslint
# - langserver: typescript-language-server
# - linter: eslint (~/.eslintrc)
# css
# - formatter: prettier
$install nodejs
$install yarn

yarn global add typescript{,-language-server}
yarn global add js-beautify prettier{,-eslint-cli}
yarn global add eslint{,-config-airbnb,-plugin{-import,-jsx-a11y,-react}}

# python
# - completer: python-language-server
# - formatter: yapf (~/.style.yapf)
# - langserver: python-language-server
# - linter: flake8 (~/.config/flake8)
$install python
$install python-pip

pip install --user python-language-server
pip install --user yapf
pip install --user flake8

# --------------------------------

# ctags
$install universal-ctags-git

# fd
$install fd-rs

# fzf
$install fzf

# font
$install ttf-hack
$install ttf-symbola

# ripgrep
$install ripgrep

# tmux
$install tmux

# vim
$install gvim

curl -fLo ~/.vim/autoload/plug.vim --create-dirs \
      https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
vim +PlugInstall +qa

mkdir -p ~/.vim/{fzf,undo}

# zsh
$install zsh

mkdir -p ~/.antigen
curl -L git.io/antigen > ~/.antigen/antigen.zsh

chsh -s $(which zsh)
