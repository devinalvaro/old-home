#!/bin/bash -v

# variables
install='trizen -S --needed --noconfirm --noedit'

# trizen
git clone https://aur.archlinux.org/trizen.git
cd trizen && makepkg -si --needed --noconfirm && cd .. && rm -rf trizen

# --------------------------------

# clang
$install clang # completer, formatter: ~/.clang-format
$install gcc-multilib # linter

# go
$install go

$install gocode-git # completer
$install goimports-git # formatter, linter

# nodejs
$install nodejs
$install yarn

yarn global add tern # completer
yarn global add js-beautify prettier{,-eslint-cli} # formatter: ~/.jsbeautifyrc
yarn global add eslint{,-config-airbnb,-plugin{-import,-jsx-a11y,-react}} # linter: ~/.eslintrc

# php
$install php
$install composer

composer global require friendsofphp/php-cs-fixer # formatter

# python
$install python
$install python-pip

pip install --user jedi # completer
pip install --user yapf # formatter: ~/.style.yapf
pip install --user flake8 # linter: ~/.config/flake8

# scheme
$install mit-scheme
$install rlwrap

# --------------------------------

# ctags
$install universal-ctags-git

# fd
$install fd-rs

# font
$install ttf-hack
$install ttf-symbola

# ripgrep
$install ripgrep

# rofi
$install rofi

# tmux
$install tmux

# vim
$install gvim
$install python-neovim

curl -fLo ~/.vim/autoload/plug.vim --create-dirs \
      https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
vim +PlugInstall +qa

mkdir -p ~/.vim/{fzf,undo}

# zsh
$install zsh

mkdir -p ~/.antigen
curl -L git.io/antigen > ~/.antigen/antigen.zsh

chsh -s $(which zsh)
