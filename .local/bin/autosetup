#!/bin/bash

# git
sudo pacman -S --noconfirm git

# home
cd ~
git init
git remote add origin https://github.com/devinalvaro/home
git fetch
git checkout -t origin/master

# clang
sudo pacman -S --noconfirm clang
sudo pacman -S --noconfirm ctags

# pip
sudo pacman -S --noconfirm python-pip
sudo pacman -S --noconfirm python2-pip

# python
pip install --user pylint
pip install --user jedi

# neovim
sudo pacman -S --noconfirm neovim
pip install --user neovim
pip2 install --user neovim

# ripgrep
sudo pacman -S --noconfirm ripgrep

# tmux
sudo pacman -S --noconfirm tmux

# xsel
sudo pacman -S --noconfirm xsel

# ranger
sudo pacman -S --noconfirm ranger

# mpv
sudo pacman -S --noconfirm mpv

# zathura
sudo pacman -S --noconfirm zathura
sudo pacman -S --noconfirm zathura-pdf-mupdf

# vim plugins
nvim -c 'PlugInstall' \
    -c 'qa!'

# fzf
.fzf/install --all

# wget
sudo pacman -S --noconfirm wget

# zsh
sudo pacman -S --noconfirm zsh

git clone git://github.com/robbyrussell/oh-my-zsh.git ~/.oh-my-zsh
git clone git://github.com/zsh-users/zsh-autosuggestions ~/.oh-my-zsh/plugins/zsh-autosuggestions

wget -o - https://raw.githubusercontent.com/denysdovhan/spaceship-zsh-theme/master/install.sh ~
chmod +x ~/install.sh
~/install.sh
rm ~/install.sh

chsh -s $(which zsh)
