#!/usr/bin/env bash
set -v

# variables
install='yay -S --needed --noconfirm --answeredit none'

# --------------------------------

# plasma
$install plasma-desktop
$install --asdeps plasma-browser-integration plasma-nm plasma-pa plasma5-applets-redshift-control

sudo systemctl enable NetworkManager.service

# decorations
$install sierrabreeze-kwin-decoration-git

# gtk
$install --asdeps breeze-gtk kde-gtk-config

# kde
$install ark dolphin kdeconnect kscreen ktorrent powerdevil spectacle
$install --asdeps kwallet-pam

# sddm
$install sddm

sudo systemctl enable sddm.service

sudo mkdir -p /etc/sddm.conf.d
sudo cp /usr/lib/sddm/sddm.conf.d/sddm.conf /etc/sddm.conf.d/sddm.conf

sudo sed -i s/Current=$/Current=breeze/ /etc/sddm.conf.d/sddm.conf
sudo sed -i s/CursorTheme=$/CursorTheme=breeze/ /etc/sddm.conf.d/sddm.conf
sudo sed -i s/EnableHiDPI=\.\*/EnableHiDPI=true/ /etc/sddm.conf.d/sddm.conf

# libraries
$install libdbusmenu-glib ntfs-3g
