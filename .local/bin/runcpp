#!/bin/bash

std=14
flag="-std=c++$std -Wall"

in='a.in'
out="$(basename $1 .cpp).out"

# ----------------

if [ $1 -nt $out ]; then
    g++ $flag $1 -o $out
fi

if [ -f $in ]; then
    ./$out < $in
else
    ./$out
fi
